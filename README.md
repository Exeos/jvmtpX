# jvmtpX

Extracts platform-specific binaries from JVMTP binary bundle files.

## Usage

```
java -jar jvmtpX.jar <path/to/bundle>
```


Each entry in the bundle is written to a file of the same name, placed in the same directory as the input file.